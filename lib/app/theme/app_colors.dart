import 'package:flutter/material.dart';

class AppColors {
  // Brand & Navigation
  static const Color primary = Color(0xFF0F172A); // Deep Navy Slate
  static const Color primaryLight = Color(0xFF1E293B);
  static const Color accent = Color(0xFF2563EB); // Vibrant Logistics Blue
  static const Color accentLight = Color(0xFF3B82F6);
  static const Color accentHover = Color(0xFF1D4ED8);

  // Background & Surfaces
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceMuted = Color(0xFFF1F5F9);
  static const Color border = Color(0xFFE2E8F0);
  static const Color borderSubtle = Color(0xFFCBD5E1);

  // Typography
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF475569);
  static const Color textMuted = Color(0xFF94A3B8);
  static const Color textInverse = Color(0xFFFFFFFF);

  // Status & Semantic Colors
  // Green - Available / Completed / Success
  static const Color green = Color(0xFF10B981);
  static const Color greenLight = Color(0xFFECFDF5);
  static const Color greenBorder = Color(0xFFA7F3D0);

  // Orange/Amber - Pending / Warning / Maintenance
  static const Color amber = Color(0xFFF59E0B);
  static const Color amberLight = Color(0xFFFFFBEB);
  static const Color amberBorder = Color(0xFFFDE68A);

  // Red - Cancelled / Breakdown / Error / Issue
  static const Color red = Color(0xFFEF4444);
  static const Color redLight = Color(0xFFFEF2F2);
  static const Color redBorder = Color(0xFFFECACA);

  // Blue - In Transit / Active / Reported
  static const Color blue = Color(0xFF3B82F6);
  static const Color blueLight = Color(0xFFEFF6FF);
  static const Color blueBorder = Color(0xFFBFDBFE);

  // Slate/Grey - Inactive / Off Duty / New
  static const Color slate = Color(0xFF64748B);
  static const Color slateLight = Color(0xFFF1F5F9);
  static const Color slateBorder = Color(0xFFE2E8F0);

  // Purple / Violet - Special tracking / CFS
  static const Color purple = Color(0xFF8B5CF6);
  static const Color purpleLight = Color(0xFFF5F3FF);
  static const Color purpleBorder = Color(0xFFDDD6FE);
}
